---
分类: Python
ask_count: 2
appear_count: 0
status: 已解决
created: 2026-08-29
---

# Python 数学

## 除法向上取整

用 `math.ceil(a / b)`，或 `-(-a // b)`。

```python
import math
print(math.ceil(7 / 2))    # 4
print(-(-7 // 2))          # 4
```

原理：`a // b` 是向下取整；`-(-a // b)` 先取负再向下取整、再取负，等价于向上取整，即 `向上取整(a/b) = -向下取整(-a/b)`。

### 提问记录

| 日期 | 来源题目 | 解答要点 |
| --- | --- | --- |
| 2026-08-29 | 无 | `math.ceil(a / b)` 或 `-(-a // b)` 向上取整 |
| 2026-08-29 | 无 | 原理：`-(-a // b)` = 先取负向下取整再取负，等价向上取整 |
